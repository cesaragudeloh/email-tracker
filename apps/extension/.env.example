# Copiar a .env.production.local después del deployment (URL pública, no secreto).
# VITE_ACTIVATION_API_URL=https://API_ID.execute-api.REGION.amazonaws.com
# En build:development se usa http://localhost:3000 si no se define una URL.
